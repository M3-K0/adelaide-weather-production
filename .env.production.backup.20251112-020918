# Adelaide Weather Forecasting System - Production Environment Variables
# Required and optional configuration for production deployment

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# API Authentication (REQUIRED)
API_TOKEN=demo-token-12345

# =============================================================================
# BUILD AND VERSION INFORMATION
# =============================================================================

# Application versioning
VERSION=1.0.0
BUILD_DATE=2025-01-14T00:00:00Z
VCS_REF=main

# =============================================================================
# SECURITY AND ACCESS CONTROL
# =============================================================================

# CORS configuration for web access
CORS_ORIGINS=http://localhost:3000,http://localhost,https://your-domain.com
TRUSTED_HOSTS=localhost,127.0.0.1,*.your-domain.com

# Grafana security
GRAFANA_PASSWORD=secure-admin-password
GRAFANA_SECRET_KEY=change-this-to-a-random-string

# Redis security (optional)
REDIS_PASSWORD=

# =============================================================================
# PERFORMANCE AND MONITORING
# =============================================================================

# Logging configuration
LOG_LEVEL=INFO

# Rate limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_ENABLED=true

# Performance optimization
COMPRESSION_ENABLED=true
COMPRESSION_MIN_SIZE=500
NGINX_COMPRESSION=true
PERFORMANCE_CACHE_TTL=300

# Monitoring configuration
PROMETHEUS_ENABLED=true
MONITORING_ENABLED=true
METRICS_COLLECTION_INTERVAL=30
CONFIG_DRIFT_REALTIME_ENABLED=true

# =============================================================================
# EXTERNAL SERVICES (OPTIONAL)
# =============================================================================

# WeatherAPI.com integration (optional enhancement)
WEATHER_API_KEY=
WEATHER_API_ENABLED=false

# =============================================================================
# INFRASTRUCTURE CONFIGURATION
# =============================================================================

# Nginx configuration
NGINX_HOST=localhost

# Redis configuration
REDIS_ENABLED=true

# Token management
TOKEN_ROTATION_ENABLED=true
TOKEN_ROTATION_INTERVAL_HOURS=24

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# 
# 1. SECURITY: Change default passwords and tokens before production deployment
# 2. WEATHER_API_KEY: Optional - system works with FAISS data only
# 3. MONITORING: Grafana accessible at http://localhost:3001
# 4. METRICS: Prometheus accessible at http://localhost:9090
# 5. LOGS: Service logs stored in ./logs/ directory
# 6. HEALTH: API health check at http://localhost/api/health
# 
# To deploy:
#   docker-compose -f docker-compose.production.yml up -d
# 
# =============================================================================